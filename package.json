{
  "name": "boss-zhipin-auto-greet",
  "version": "2.0.0",
  "description": "BOSS直聘自动打招呼 Tampermonkey 脚本（模块化开发）",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "tm:dev": "node scripts/tm-dev.mjs",
    "sync": "node scripts/sync.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "rollup": "^4.0.0",
    "vitest": "^4.1.1"
  }
}
